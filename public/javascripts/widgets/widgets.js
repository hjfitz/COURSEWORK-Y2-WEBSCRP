const weatherSettingIcon = document.getElementById('weather-setting-icon')

weatherSettingIcon.addEventListener('click', setLocation)

function setLocation() {
  //show map as overlay, enable selection
}
